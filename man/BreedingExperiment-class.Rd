% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R, R/show.R
\docType{class}
\name{BreedingExperiment-class}
\alias{BreedingExperiment-class}
\alias{show,BreedingExperiment-method}
\title{Breeding experiment container}
\usage{
\S4method{show}{BreedingExperiment}(object)
}
\arguments{
\item{object}{A \link[=BreedingExperiment-class]{BreedingExperiment} object.}
}
\description{
A \code{BreedingExperiment} keeps everything a quantitative-genetic analysis needs
in one object: marker genotypes, the phenotypes and design variables of the
individuals, the pedigree, and the position of every marker on the genome.
}
\details{
The class extends
\link[SummarizedExperiment:RangedSummarizedExperiment-class]{SummarizedExperiment::RangedSummarizedExperiment}, so markers are rows
and individuals are columns, and the familiar accessors work as usual:
\code{assay()} returns the genotype matrix, \code{rowRanges()} the marker coordinates
as a \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges}, and \code{colData()} the phenotypes. Because
marker positions are genomic ranges, a breeding data set can be subset by
region, overlapped with annotation, and otherwise handled with the standard
Bioconductor vocabulary.

One slot is added to the parent class:

\describe{
\item{\code{pedigree}}{A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with columns \code{id}, \code{sire} and
\code{dam}, giving the parents of each individual. Unknown parents are \code{NA}.
It may contain ancestors that were never genotyped, which is what makes
single-step analysis possible.}
}

Genotypes are stored as allele dosages: 0, 1 or 2 copies of the counted
allele, with \code{NA} for missing calls.
}
\section{Construction}{

Use \code{\link[=BreedingExperiment]{BreedingExperiment()}}; see its help page for examples.
}

\section{Display}{

The \code{show} method prints the size of the object, the assays it holds, the
phenotype columns, the sequences the markers lie on, and a summary of the
pedigree including how many individuals are genotyped.
}

\examples{
data(demoBreeding)
demoBreeding

}
\seealso{
\code{\link[=BreedingExperiment]{BreedingExperiment()}}, \code{\link[=Amatrix]{Amatrix()}}, \code{\link[=Gmatrix]{Gmatrix()}}, \code{\link[=Hmatrix]{Hmatrix()}}
}
