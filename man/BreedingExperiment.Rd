% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor.R
\name{BreedingExperiment}
\alias{BreedingExperiment}
\title{Create a breeding experiment}
\usage{
BreedingExperiment(
  genotypes,
  phenotypes = NULL,
  pedigree = NULL,
  rowRanges = NULL,
  ...
)
}
\arguments{
\item{genotypes}{A numeric matrix of allele dosages, markers in rows and
individuals in columns, with column names identifying the individuals.}

\item{phenotypes}{Optional data frame or \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} of
individual-level variables, with one row per column of \code{genotypes}.}

\item{pedigree}{Optional data frame with columns \code{id}, \code{sire} and \code{dam}.
Unknown parents may be \code{NA} or \code{0}.}

\item{rowRanges}{Optional \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} giving the position
of each marker, of the same length as \code{nrow(genotypes)}.}

\item{...}{Further assays, passed to
\code{\link[SummarizedExperiment:SummarizedExperiment]{SummarizedExperiment::SummarizedExperiment()}}.}
}
\value{
A \link[=BreedingExperiment-class]{BreedingExperiment} object.
}
\description{
Builds a \link[=BreedingExperiment-class]{BreedingExperiment} from a marker genotype matrix, with
optional phenotypes, pedigree and marker coordinates.
}
\details{
Genotypes are allele dosages: usually the integers 0, 1 and 2, with \code{NA} for
missing calls. Fractional values in between are accepted, so expected dosages
from imputation can be stored directly. Markers are rows and individuals are
columns, matching the Bioconductor convention that features are rows.

If \code{rowRanges} is not supplied, markers are given placeholder coordinates on
a single sequence so that the object is still a valid ranged experiment; pass
real coordinates whenever you have them, because that is what allows a data
set to be subset by genomic region.

The pedigree may name ancestors that were never genotyped. Those extra
individuals are exactly what \code{\link[=Hmatrix]{Hmatrix()}} needs in order to combine pedigree
and genomic information.
}
\examples{
set.seed(1)
geno <- matrix(rbinom(40 * 12, 2, 0.3), nrow = 40, ncol = 12,
               dimnames = list(paste0("snp", 1:40), paste0("ind", 1:12)))
pheno <- data.frame(yield = rnorm(12))
be <- BreedingExperiment(geno, phenotypes = pheno)
be
dim(be)
}
\seealso{
\code{\link[=Gmatrix]{Gmatrix()}}, \code{\link[=Amatrix]{Amatrix()}}, \code{\link[=Hmatrix]{Hmatrix()}}, \code{\link[=simulateBreeding]{simulateBreeding()}}
}
