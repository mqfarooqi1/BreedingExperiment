% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{demoBreeding}
\alias{demoBreeding}
\title{A demonstration breeding population}
\format{
A \link[=BreedingExperiment-class]{BreedingExperiment} with 900 markers and 120 genotyped
individuals, and a pedigree of 180. \code{colData} holds:
\describe{
\item{generation}{Generation number, 3 to 5 for genotyped individuals.}
\item{sex}{\code{"F"} or \code{"M"}.}
\item{yield}{A moderately heritable trait (\eqn{h^2 \approx 0.35}),
recorded on females only.}
\item{stature}{A more heritable trait (\eqn{h^2 \approx 0.60}), recorded
on everyone.}
\item{trueBV_yield, trueBV_stature}{The simulated true breeding values.}
}
}
\source{
Simulated by \code{data-raw/make_demo.R} in the package sources.
}
\usage{
data(demoBreeding)
}
\description{
A small, five-generation breeding population supplied so that every function
in the package can be tried on data that behaves like a real programme.
}
\details{
The data are \strong{simulated}, not observed. They are built to have the awkward
features that real breeding data has and that tidy textbook examples lack:

\itemize{
\item \strong{Only part of the population is genotyped.} The two earliest
generations were born before genotyping began, so 60 of the 180
individuals in the pedigree have no markers. This is the situation
\code{\link[=Hmatrix]{Hmatrix()}} exists for.
\item \strong{Unequal family sizes}, because a few sires are used heavily.
\item \strong{A trait recorded on one sex only.} \code{yield} is missing for males,
as a milk trait would be.
\item \strong{Missing genotype calls}, at about one per cent, as any chip has.
\item \strong{Markers on ten chromosomes} with irregular spacing, so subsetting
by genomic region is meaningful.
}

Because the data are simulated, the true breeding values are known and are
kept in \code{colData}, which makes the object useful for checking that a
prediction method recovers what it should.
}
\examples{
data(demoBreeding)
demoBreeding

# 60 of the 180 individuals in the pedigree were never genotyped
nrow(pedigree(demoBreeding)) - ncol(demoBreeding)

# the single-step matrix covers all of them
H <- Hmatrix(demoBreeding)
dim(H)
}
\seealso{
\code{\link[=simulateBreeding]{simulateBreeding()}} to generate your own, \code{\link[=Hmatrix]{Hmatrix()}} for the
analysis this data set is shaped for.
}
\keyword{datasets}
